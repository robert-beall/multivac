services:
  database:
    image: postgres:18
    env_file: 
      - database/.env.local
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql
  backend: 
    build: ./backend
    ports:
      - "8080:8080"
    depends_on:
      - database
volumes:
  dbdata: